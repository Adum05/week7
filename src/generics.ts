//let array: number[]= [1,2];

let array= new Array<number>();
array.push(1, 2);
//array.push(`string value`)

array.forEach(value => {
    value.
});


let map = new Map<string, string>();
map.set(`NE`, `Nebraska`);
map.set(`IA`, `Iowa`);
//map.set(1,3);

let stateName= map.get(`NE`)